apiVersion: v1
kind: Service
metadata:
  name: {{ template "uow.fullname" . }}
  labels:
    app: {{ template "uow.name" . }}
    chart: {{ template "uow.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "uow.name" . }}
    release: {{ .Release.Name }}
